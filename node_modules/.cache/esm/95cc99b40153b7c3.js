let Product,multer,path,CustomErrorHandler,fs,joi,productSchema;_015‍.x([["default",()=>_015‍.o]]);_015‍.w("../models",[["Product",["Product"],function(v){Product=v}]]);_015‍.w("multer",[["default",["multer"],function(v){multer=v}]]);_015‍.w("path",[["default",["path"],function(v){path=v}]]);_015‍.w("../services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_015‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_015‍.w("joi",[["default",["joi"],function(v){joi=v}]]);_015‍.w("../validators/productValidator",[["default",["productSchema"],function(v){productSchema=v}]]);







const storage= multer.diskStorage({
    destination: (req,file,cb)=> cb(null,'uploads'),
    filename: (req,file,cb)=> {
           const uniqueName=`${Date.now()}-${Math.round(Math.random() * 1E9)}${path.extname(file.originalname)}`;
        cb(null,uniqueName)
    }
})

const handleMultipartData= multer({storage, limits: {fileSize: 1000000 * 5}}).single('image') //5mb file size


const productController={
    async store(req,resp,next){
        //Multipart Form Data
      handleMultipartData(req,resp, async (err)=> {
         if(err){
            return next(CustomErrorHandler.serverError(err.message));
        }
        
        const filePath= req.file.path;
         _015‍.g.console.log(req.file);
         //validation
       
        
        const {error} = productSchema.validate(req.body);
        if(error){
            // delete the upload file image
            fs.unlink(`${appRoot}/${filePath}`, (err)=>{
                if(err){

                    return next(CustomErrorHandler.serverError(err.message));
                }
             });

             return next(error)
         }

         const {name, price, size}= req.body;

        let document;
      
         try{

              document = await Product.create({
                name,
                price,
                size,
                image: filePath
              });

         } catch(err){

           return next(err);
         }

           resp.status(201).json(document);
      });

    },

    //Update Data .......................................................................................
    update(req,resp,next){
      handleMultipartData(req,resp, async (err)=> {
        if(err){
           return next(CustomErrorHandler.serverError(err.message));
       }
       
       //  console.log(req.file);
        let filePath;
       if(req.file){

          filePath= req.file.path;

       }
       
       
        //validation
        const productSchema= joi.object({
           name: joi.string().required(),
           price: joi.number().required(),
           size: joi.string().required()


       });
       
       const {error} = productSchema.validate(req.body);
       if(error){
           // delete the upload file image
          if(req.file){

            fs.unlink(`${appRoot}/${filePath}`, (err)=>{
              if(err){

                  return next(CustomErrorHandler.serverError(err.message));
              }
           });
          }

            return next(error)
        }

        const {name, price, size}= req.body;

       let document;
     
        try{

             document = await Product.findOneAndUpdate({_id:req.params.id},{
               name,
               price,
               size,
               ...(req.file && { image: filePath})
             }, { new: true });
             _015‍.g.console.log(document)

        } catch(err){

          return next(err);
        }

          resp.status(201).json(document);
     });

    },

    //delete the product..............................................................
    async destroy(req,resp,next){
 
      const document= await Product.findOneAndRemove({_id: req.params.id});
      if(!document){

        return next(new Error('Nothing to delete'))
      }
      //image delete
      const imagePath= document._doc.image;
      // console.log(imagePath);
      fs.unlink(`${appRoot}/${imagePath}`, (err)=>{
       
        if(err){
          return next(CustomErrorHandler.serverError());
        }
        
       return resp.json(document);      
      });

    },

    // Get all products 
    async index(req,resp,next){
      let documents;
      // Pagination mongoose-pagination for more than thousand products
      try{
            
            documents = await Product.find().select('-updatedAt -__v');

      } catch(err){

       return next(CustomErrorHandler.serverError()); 

      }
       
       return resp.json(documents)  

   },

   // Show one Product

   async show(req, resp, next){
         
    let document;

    try{

      document= await Product.findOne({_id: req.params.id}).select('-updatedAt -__v')

    } catch(err){

    return next(customElements.serverError());

    }

    return resp.json(document);

   },

   async getProducts(req, res, next) {
    let documents;
    try {
        documents = await Product.find({
            _id: { $in: req.body.ids },
        }).select('-updatedAt -__v');
    } catch (err) {
        return next(CustomErrorHandler.serverError());
    }
    return res.json(documents);
},
};



_015‍.d(productController);









