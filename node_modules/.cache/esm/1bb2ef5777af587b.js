let JWT_SECRET,jwt;_77a‍.x([["default",()=>_77a‍.o]]);_77a‍.w("../config",[["JWT_SECRET",["JWT_SECRET"],function(v){JWT_SECRET=v}]]);_77a‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);




class JwtService {

    static sign(payload, expiry = '60s', secret = JWT_SECRET) {
        
        return jwt.sign(payload,secret, {expiresIn: expiry})
    }

    static verify(payload,  secret = JWT_SECRET) {
        
        return jwt.verify(payload,secret)
    }
}

_77a‍.d(JwtService);